/**
 * Queue stubs — Redis/BullMQ removed.
 * Submissions are processed inline via Judge0 (no Redis dependency).
 */
export const connection = null;
export const submissionQueue = null;
export const redisAvailable = false;
